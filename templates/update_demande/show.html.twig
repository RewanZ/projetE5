{% extends 'base.html.twig' %}

{% block title %}Demande{% endblock %}

{% block body %}
    <h1>Demande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ demande.id }}</td>
            </tr>
            <tr>
                <th>Date_updated</th>
                <td>{{ demande.dateUpdated ? demande.dateUpdated|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin_demande</th>
                <td>{{ demande.dateFinDemande ? demande.dateFinDemande|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ statutTab[demande.status] }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_update_demande_index') }}">back to list</a>

    <a href="{{ path('app_update_demande_edit', {'id': demande.id}) }}">edit</a>

    {{ include('update_demande/_delete_form.html.twig') }}
{% endblock %}
